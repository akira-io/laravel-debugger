{
  "name": "akira-laravel-debugger",
  "version": "1.1.0",
  "description": "Advanced debugging and diagnostics tools for Laravel applications",
  "private": true,
  "scripts": {
    "lint": "composer analyse",
    "test": "composer test",
    "release": "release-it",
    "release:dry": "release-it --dry-run",
    "release:ci": "release-it --ci"
  },
  "devDependencies": {
    "release-it": "^19.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
